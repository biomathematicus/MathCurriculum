1. Fix Route Matrix. (PARTIALLY COMPLETE, but we need 4. done first.)

2. Input of specific courses as arguments to the CurriculumGraph class. (PARTIALLY COMPLETE)

3. Write our own Adjanency matrix algorithm.  The nx one is weird.

4. Make a dict of what each number in the graph represents.

5. Import Google Sheets automatically.

6. See if we can have the display be tunable to whether it displays lesson or number representation.

7. Change every function possible to use *args and then keep types as strings.

------ Non-Programming Related ------
NP1. Get more course information from instructors to fill out the missing courses.
